// Creating a variable for each of your friends would make your code long and complex.
const friend1 = "Tom";
const friend2 = "Mia";
const friend3 = "Lee";

// Rather than creating a variable for each friend's name, we can group all of the values into an array using [ and ].
const friends = ["Tom", "Mia", "Lee"];
console.log(friends);

// The values of an array are called elements. For example, "Tom", "Mia", and "Lee" are elements of this array.
const friends2 = ["Tom", "Mia", "Lee"];
console.log(friends2);

// To create an empty array, we code [ and ].
const friends3 = [];
console.log(friends3);
